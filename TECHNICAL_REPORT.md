# 基于单目视频与物理约束 IK 的通用人形机器人动作重定向系统

> **【项目核心标签】** 从一段单目视频出发，端到端输出任意人形机器人的关节角序列——**实时、可落地、多机型零代码扩展**。

---

## 一、我们解决了什么难题（先看这一栏，3 分钟建立信任）

评委没时间找亮点，但一眼能看出致命伤：穿模、抖动、漂移、延迟。我们把它们提前解决并摊开写清楚。

| # | 致命难题 | 为什么一眼就露馅 | 我们的方案 | 效果（含锚点） |
|---|---------|----------------|-----------|---------------|
| 1 | **脚底穿地板（穿模）** | 机器人"陷进"地面 = 没做完 | `height_adjust` 按**鞋底网格**（非脚踝坐标系）校准离地高度 | 鞋底穿透 **7.6cm → ≈0**，无漂浮、无穿透 |
| 2 | **WHAM 滑窗重置导致根轨迹断裂 / 朝向跳变** | 机器人原地漂移、突然抽搐 | 根速度**积分重建连续骨盆轨迹** + 四元数 **15°/帧限幅** + 位移阈值拒跳 | 朝向跳变消除、轨迹连续（见 §四对比图） |
| 3 | **坐标系统一（y-up vs z-up）** | 人仰马翻、朝向错乱 | 根旋转 / 位移的 y-up→z-up **显式变换** | 各机型一致落地，无坐标系 bug |
| 4 | **多机型适配成本高** | 换个机器人就要重写代码 | JSON 驱动的 **IK 映射表**（人关节 ↔ 机器人 body / 权重 / 偏置） | **20+ 机型零代码接入**；新增 linglong2 仅需一条注册 |
| 5 | **CUDA 运行时架构不匹配崩溃** | Demo 现场崩 = 灾难 | 后处理模块自动检测 NVRTC 架构错误并**回退 CPU** | 现场永不因算力环境崩掉 |
| 6 | **脚底悬空（漂浮）** | 压步/轻点等动作脚悬半空，一眼"浮空感" | WHAM 脚掌(ball)高度不可靠（支撑脚脚尖朝下、摆动脚脚尖翘），而**脚踝可靠** → 源侧按"踝−6.7cm"重算 ball 高度 | 悬空 **7cm → ≈2cm**，摆动帧不误伤 |
| 7 | **跨步中途"踏空"（摆动脚回抽弹起）** | 落地前摆动脚凭空抬升又回落（前脚 9.6cm 大弹、后脚 ~8mm 小弹），一眼"踩空" | 源侧**三态机单脚滤波**（planted→up→down，峰值相对触发）：双脚分别只向下拉，压回摆动下降段的最低持握高度 | 前脚弹起 **9.6cm → 1.4cm**、后脚小弹 **~8mm → ≈0**；真实摆动帧（13~14cm 摆幅）零误伤 |
| 8 | **脚尖微穿模（脚趾戳地）** | 运动/压步中脚尖下沉几毫米戳进地面，一眼"没站稳" | 支撑脚踝**迭代展平**（单步→收敛，消除残余前倾）+ 按鞋底网格实测最低点做**有界抬升**（≤1cm，不平移深蹲） | 任意穿模帧 **431→80**，毫米级"微微穿模"基本清零；深蹲 5cm 级残留为踝关节行程**硬件极限** |

> 第 1、2 行是"工程落地经验"的证明：不是纸上谈兵，是真踩过坑、真修好了。

---

## 二、核心指标（每个数字都带锚点，拒绝孤立自夸）

| 指标 | 数值 | 锚点（让评委瞬间定位你的水平） |
|-----|------|-------------------------------|
| 端到端延迟 | 【待实测】ms | 低于**人类感知阈值 200ms**；实时目标 33ms/帧 |
| 姿态误差 MPJPE | 【待实测】mm | 较 WHAM 直接前馈基线**下降 __%** |
| 足底离地高度 | 穿模 **-7.6→~0**cm；悬空 **7→≈2**cm | 悬空源于 WHAM 脚掌(ball)估测误差，踝驱动校正后落地 |
| 踏空（摆动脚回抽） | 前脚 **9.6→1.4**cm；后脚 **~8mm→≈0** | 源侧峰值相对三态机，双脚分别滤波，真实摆动帧零误伤 |
| 朝向跳变（帧间） | ≤ 15°/帧 | 未加约束时滑窗重置可达 **90°+ 瞬跳** |
| 支持机型数 | 20+ | 覆盖宇树 G1/H1、灵巧、x02lite、LingLong2 等 |
| 帧率 | 【待实测】fps | 达到/超过视频源帧率（30fps 实时） |

> 填数字口诀：**左侧放你的值，右侧放"对比锚点"**（比基线好多少 / 低于什么标准线）。

---

## 三、技术架构（一图看懂流水线）

```
单目视频
   │  WHAM（世界坐标系人体姿态估计，SMPL-X）
   ▼
人体关节位姿（骨盆 / 髋 / 膝 / 踝 / 肩 / 肘 / 腕 / 头）
   │  ① 坐标系统一（y-up → z-up）  ② 根速度积分 → 连续轨迹
   │  ③ 四元数限幅 → 抗跳变      ④ 指数平滑 → 抗抖动
   │  ⑤ 脚掌贴平（ball 按踝校正）→ 抗悬空
   │  ⑥ 踏空抑制（三态机单脚滤波，前/后脚分别处理）→ 抗踏空
   ▼
GMR 逆运动学重定向（mink 求解，双层任务 task1 + task2）
   │  配置驱动：人关节 ↔ 机器人 body 的 IK 映射 JSON
   ▼
机器人关节角序列 qpos（30 DoF / 机型自适应）
   │  后处理：鞋底贴地校准 + 平滑
   ▼
MuJoCo 可视化 / CSV 下发 / TCP 实时流（Unity）
```

**一句话架构**：`WHAM 看人 → 稳定性后处理 → GMR 配机器人 → 关节角下发`。

---

## 四、鲁棒性：这不是"最优美的那一张图"

技术比赛最怕 Demo 崩。我们在**脏数据、复杂环境**下验证，而非只放最优一帧。

> 📷 **【待插入对比图】极端场景对比**
>
> | 场景 | 基线（崩溃态） | 本方案（稳定态） |
> |------|--------------|----------------|
> | 遮挡 / 低光照 | 【图】脚陷地、根漂移 | 【图】脚贴地、轨迹连续 |
> | 快动作 / 相机抖动 | 【图】朝向抽搐 | 【图】平滑跟随 |
>
> 视觉上的"碾压感"比一万字描述都有力——**务必放这张对比图**。

鲁棒性来自四处工程兜底：
1. **根速度积分**：WHAM 滑窗重置只重置了局部坐标，我们靠速度积分重建全局轨迹。
2. **朝向限幅 + 位移阈值**：拒绝跳变帧，而不是盲目跟随脏数据。
3. **鞋底贴地校准**：按真实网格几何（而非坐标系原点）定高度。
4. **脚掌贴平校准**：WHAM 的脚踝高度可靠而脚掌(ball)不可靠，接触帧内按踝重算 ball 高度，消除脚底悬空。
5. **踏空抑制**：跨步中途的"回抽弹起"是纯垂直方向的深度估计伪影（脚的水平位置静止、仅高度抬升再回落），用峰值相对的三态机在源侧分别把前脚、后脚向下拉回摆动下降段的最低持握高度，压掉踏空而不伤真实摆动。
6. **脚尖贴地校准**：支撑脚踝关节由"单步 60% 修正"改为**迭代收敛**到脚底水平，再加按鞋底网格实测最低点的**有界抬升**（≤1cm），把运动中的毫米级"脚尖微微穿模"压掉；抬升有界，故深蹲时踝关节已到行程上限（-0.803 rad 仍脚尖朝下）的 5cm 级穿透不做整体抬升——那会反过来让后跟悬空。

---

## 五、关键技术细节（评委追问时能答上来）

- **WHAM**：World-grounded Humans，单目视频 → 世界坐标系下的 3D 人体姿态，输出 SMPL-X 参数（body pose / betas / root orient / trans）。
- **GMR**：基于 `mink` 的逆运动学求解（DAQP 求解器 + 阻尼 + 关节限位），双层任务（task1 主、task2 精修）迭代收敛。
- **配置驱动映射**：`smplx_to_<robot>.json` 定义人关节 → 机器人 body 的对应关系、位置/朝向权重、偏置、比例表。**新增机型 = 新增一张 JSON 表，不改一行求解代码**。
- **在线后处理**：指数平滑（抗抖）、鞋底贴地（抗穿模）、四元数连续性（抗跳变）、原点归零（抗漂移）。
- **源侧脚掌贴平**：WHAM 的脚踝高度可靠、脚掌(ball)不可靠（支撑脚脚尖朝下、摆动脚脚尖翘），接触帧内把 ball 高度重算为"踝 − 6.7cm"的平脚关系，消除压步/轻点时的脚底悬空。
- **踏空抑制（三态机，双脚）**：`planted → up → down` 三态；某脚踝抬升超过 latch 进入摆动相，越过摆动峰值并回落 `descend` 幅度后进入 down，记录**运行最低高度**，一旦反弹回升（> `relift_eps`）就把整只脚（踝 + 球）向下拉回最低持握高度，低于 release 判定真正落地，超 `max_hold` 帧自动释放。触发是**峰值相对**的，故能同时压掉前脚的大弹（近地 9.6cm）与后脚的小弹（下降中段 ~8mm），又不误伤峰值略高于 latch 的浅摆动。左右脚各自独立状态、只向下修正，另一脚与真实摆动帧完全不受影响。
- **脚尖防穿模（迭代展平 + 有界抬升）**：`height_adjust` 用固定 `sole_offset` 把最低**身体坐标系**（踝）对齐到地面，但脚一旦前倾几度，脚尖网格就比中性位姿更低、戳进地面。修正分两层：① 支撑脚踝的展平由"单步 `blend`"改为**牛顿迭代收敛**（消除残余前倾，且受关节行程约束）；② 再按鞋底网格实测最低点做**有界抬升**（`max_lift=1cm`，跨帧平滑）补掉展平也转不掉的毫米级残余。抬升设界，故深蹲（踝已到 toe-up 极限、脚尖仍朝下 33°）的 5cm 级穿透不会被整体抬升"藏"成后跟悬空，作为硬件行程极限保留。

---

## 六、落地验证（证明"活儿干完了"）

- [x] 端到端流程跑通：`视频 → WHAM → GMR → 关节角 → 可视化/下发`
- [x] 多机型实测：宇树 G1、x02lite、LingLong2 等
- [x] 实时流输出：MuJoCo 逐帧渲染 / CSV 下发 / TCP 流（Unity 侧接收）
- [x] 逐帧截图 & 视频录制：可复现、可留档

---

## 七、给评委的一句话（可直接抄进评语）

> **"这是一套单目视频驱动的通用人形机器人实时动作重定向系统：用 WHAM 完成世界坐标系人体姿态估计，用配置驱动的 mink IK 完成多机型零代码适配，并用速度积分、朝向限幅、鞋底贴地、脚掌贴平、踏空抑制五项工程约束解决了穿模、漂移、跳变、悬空、踏空五类落地难题。"**
